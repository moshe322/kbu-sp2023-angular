import { SortByUnitPricePipe } from './sort-by-unit-price.pipe';

describe('SortByUnitPricePipe', () => {
  it('create an instance', () => {
    const pipe = new SortByUnitPricePipe();
    expect(pipe).toBeTruthy();
  });
});
