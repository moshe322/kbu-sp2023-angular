import { PriceChangeDirective } from './price-change.directive';

describe('PriceChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new PriceChangeDirective();
    expect(directive).toBeTruthy();
  });
});
