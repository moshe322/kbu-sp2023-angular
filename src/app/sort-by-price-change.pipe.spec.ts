import { SortByPriceChangePipe } from './sort-by-price-change.pipe';

describe('SortByPriceChangePipe', () => {
  it('create an instance', () => {
    const pipe = new SortByPriceChangePipe();
    expect(pipe).toBeTruthy();
  });
});
